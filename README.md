
  # AniSearch IL

  This is a code bundle for AniSearch IL. The original project is available at https://www.figma.com/design/9oc2dhxIjqs4meDMsvtNJN/AniSearch-IL.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  